{
  "name": "Ubuntu Base Test",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  // Remove the "features" section for now
  // Remove the "postCreateCommand" for now
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.flutter", // Keep extensions, they don't affect the build
        "Dart-Code.dart-code"
      ]
    }
  },
  "terminal.integrated.defaultProfile.linux": "bash",
  "terminal.integrated.profiles.linux": {
    "bash": {
      "path": "/bin/bash"
    }
  }
}